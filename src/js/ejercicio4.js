const base = 8;
const altura = 5;

const area = base * altura;
const perimetro = 2 * (base + altura);

console.log(`La base es ${base} cm`);
console.log(`La altura es ${altura} cm`);
console.log(`El area es ${area} cm cuadrados`);
console.log(`El perimetro es ${perimetro} cm`);